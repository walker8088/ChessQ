import os
import shutil
from pathlib import Path

'''
for file in Path('.').glob('**/*.xqf'):
    new_file = f'{file.parent}_{file.name}' 
    shutil.copy(file, new_file)
    #for file in files:   
    #    print(root, file)
'''    